# User CRUD is exposed through the role-specific students/teachers viewsets
# (a User only ever exists alongside a Student or Teacher profile, except for
# admins who are provisioned directly — see authentication.views.AdminUserCreateView).
